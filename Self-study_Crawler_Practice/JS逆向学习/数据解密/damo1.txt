JS逆向学习--使用标准加密算法的网站解密方式(decrypt())
要求：
decrypt()必须为实例对象中的实例方法。不能是形如class().decrypt()
在进行反向爬取JS代码时的技巧：
1.首先进行运行，并根据报错信息来一步步确定参数的内容。
2.一定要注意报错内容的特点，是函数形式还是字符串形式，也就是说要看清报错的参数处于哪个位置。
3.可以将报错参数直接方式console中。
4.在这个实践中，参数h在JS代码中为一个function(){return t}的形式，即此形式为一个空函数，那么在定位
具体的内容时需要向上思考，找到真正的起点。